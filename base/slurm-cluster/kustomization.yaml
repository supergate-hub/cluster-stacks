apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - auth-secrets.yaml
  - controller.yaml
  - nodeset.yaml
  - restapi.yaml

commonLabels:
  app.kubernetes.io/part-of: launcher-slurm
