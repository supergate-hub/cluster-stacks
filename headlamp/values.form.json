{
  "type": "object",
  "required": true,
  "defaultValue": {},
  "viewSpec": {
    "type": "base",
    "order": ["configuration"]
  },
  "properties": {
    "configuration": {
      "type": "object",
      "required": true,
      "defaultValue": {},
      "viewSpec": {
        "type": "base",
        "layout": "section",
        "layoutTitle": "Access",
        "order": ["clusterRoleName"]
      },
      "properties": {
        "clusterRoleName": {
          "type": "string",
          "required": true,
          "defaultValue": "view",
          "enum": ["view", "cluster-admin"],
          "enumNames": ["Read-only (recommended)", "Full cluster administration"],
          "viewSpec": {
            "type": "select",
            "layout": "row",
            "layoutTitle": "Cluster access",
            "layoutDescription": "Creates a ClusterRoleBinding for the Headlamp service account."
          }
        }
      }
    }
  }
}
